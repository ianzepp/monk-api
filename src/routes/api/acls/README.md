# ACLs API Notes

This file is legacy implementation background for the ACLs API.
The current public contract lives in [PUBLIC.md](./PUBLIC.md).

Use the route docs for the live API shape:
- `GET /api/acls/:model/:id`
- `POST /api/acls/:model/:id`
- `PUT /api/acls/:model/:id`
- `DELETE /api/acls/:model/:id`

This note is kept for historical context only.
