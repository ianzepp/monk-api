# User API Notes

This file is legacy implementation background for the User API.
The current public contract lives in [PUBLIC.md](./PUBLIC.md).

Use the route docs for the live API shape:
- `GET /api/user/me`
- `PUT /api/user/me`
- `DELETE /api/user/me`
- `GET /api/user`
- `POST /api/user`
- `GET /api/user/:id`
- `PUT /api/user/:id`
- `DELETE /api/user/:id`
- `POST /api/user/sudo`

This note is kept for historical context only.
