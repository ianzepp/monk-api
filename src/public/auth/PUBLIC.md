# Public Authentication API (Merged)

Public authentication documentation now lives alongside the protected Auth API reference so both flows stay in sync.

👉 See `src/routes/auth/PUBLIC.md` (or `/docs/auth`) for the combined Auth API guide covering both public and protected endpoints.
