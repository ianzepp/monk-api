{
  "name": "imports",
  "description": "Data import and restore pipeline (restores, runs, logs)",
  "version": "1.0.0",
  "is_system": false,
  "dependencies": ["system"],
  "models": 3,
  "features": [
    "data-import",
    "restore-pipeline"
  ]
}
