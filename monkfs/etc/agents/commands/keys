keys - Manage API keys and secrets

Usage:
  keys list              List all keys
  keys get <name>        Get key value
  keys set <name> <val>  Set key value
  keys delete <name>     Delete key

Examples:
  keys list
  keys set OPENAI_API_KEY sk-...
  keys get STRIPE_SECRET
  keys delete OLD_KEY

Keys are stored securely and available as environment variables.
