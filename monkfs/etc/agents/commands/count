count - Count records in a model

Usage:
  count [path]

Examples:
  count users
  count /api/data/orders
  count                     (current directory)

Returns just the number, useful for quick checks.
