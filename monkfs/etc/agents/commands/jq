jq - JSON query and transform

Usage:
  jq <filter> [file]
  <input> | jq <filter>

Common filters:
  .              Identity (pretty print)
  .field         Extract field
  .[]            Iterate array
  .[0]           First element
  .field[]       Iterate nested array
  select(.x>1)   Filter
  {a,b}          Pick fields
  length         Array/string length

Examples:
  cat record.json | jq .name
  jq '.[] | {id, name}' users.json
  curl /api/data/users | jq '.[0]'
