delete - Remove records (soft delete)

Usage:
  delete <path>

Examples:
  delete /api/data/users/<id>
  delete .                          (current record)

Records are soft-deleted (moved to trash).
Use /api/trashed to restore or permanently delete.
