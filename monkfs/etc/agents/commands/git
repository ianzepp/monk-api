git - Version control for data

Usage:
  git status             Show uncommitted changes
  git log [n]            Show commit history
  git diff [id]          Show changes
  git commit -m "msg"    Commit staged changes
  git checkout <id>      Restore to commit

Examples:
  git status
  git log 5
  git diff
  git commit -m "Updated user records"

Tracks changes to /api/data records.
