# Test Environment Configuration
# Uses port 9001 (single port for all environments)

# Database - REQUIRED
# Test uses 'monk_test' database (isolated from production and development)
#DATABASE_URL=postgresql://ianzepp@localhost:5432/monk_test
DATABASE_URL=postgresql://ianzepp@localhost:5432/monk

# Server
PORT=9001
NODE_ENV=test

# Test configuration
TEST_API_URL=http://localhost:9001
TEST_PORT=9001

# JWT Secret
JWT_SECRET=test

# Tenant Naming - Database names are always generated using SHA256 hashing
# This ensures environment isolation and prevents naming conflicts
